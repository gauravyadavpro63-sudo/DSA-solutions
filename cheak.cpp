#include <bits/stdc++.h>
using namespace std;

bool palindrome(string s) {
        if(s.size()==1){
            return true;
        }
        for(int a=0;a<=s.size()/2;a++){
            
        if(a>=s.size()/2){
            return true;
        }
        else if(s[a]!=s[s.size()-a-1]){
            return false;
        }
        
        }
    }


int main(){
    // string s;
    // cin>>s
    // cout<<palindrome(s);
    // int box[3];
    int box[3]={1,2,3};
    int a[5];
    cout<<box[];
}